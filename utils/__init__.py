"""Utility helpers (logging, configuration, common functions).

Shared utilities for the project.

Submodules:
    cache            – get_cache() wrapper around diskcache.
    gpu              – GPU detection/setup.
    math             – numerical helpers (e.g., calculate_posture_score).
    image_compress   – ROI-aware frame compression.
"""
